# Rust callers — `regex-syntax-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `rust_reexport_graph.json`. Do not hand-edit. Re-run the generator after rebuilding the graph.

## What this file is

For every Rust crate vendored under `regex-syntax-kotlin/tmp/`, this file lists:

- **Inbound callers** — every other `*-kotlin` repo's Rust source that brings a symbol from one of this repo's crates into scope via `use` or `pub use`, grouped by the imported symbol.
- **Re-export surface** — every `pub use` line this repo's crates emit, with source file:line and a flag marking the ones that live in `mod.rs` or `lib.rs` (the canonical re-export sites the migration rule targets).

**Granularity is import-level, not call-site-level.** A row shows that some other crate `use`'d the symbol; what that crate then *does* with the name (calls it, implements it, takes it as a generic bound, pattern-matches on it) lives in the importer's source body. For call-site detail, grep the listed importer files for the imported name. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project migration concern this file exists to surface.

## Crates in this repo

- `regex-syntax` (module `regex_syntax`) — 10 `pub use`, 20 cross-repo inbound edges from 3 other repos

## `regex-syntax`

- Repo path: `regex-syntax-kotlin/tmp/regex-syntax`
- Module path (Rust): `regex_syntax`
- `pub use` count: 10
- Inbound `use` edges (workspace-wide, including intra-repo): 21

### Cross-repo inbound — 20 edges, 12 distinct symbols, 3 caller repos

#### `regex_syntax` — 1 edges, 1 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/re/mod.rs:4` — `use regex_syntax`

#### `regex_syntax::Error` — 1 edges, 1 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/re/mod.rs:4` — `use regex_syntax::Error`

#### `regex_syntax::ParserBuilder` — 3 edges, 3 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/re/mod.rs:4` — `use regex_syntax::ParserBuilder`
- **`logos-kotlin` / `logos`**
  - `tmp/.../logos-codegen/src/pattern.rs:4` — `use regex_syntax::ParserBuilder`
- **`logos-kotlin` / `logos-codegen`**
  - `tmp/.../src/pattern.rs:4` — `use regex_syntax::ParserBuilder`

#### `regex_syntax::escape` — 1 edges, 1 caller crates

- **`syntect-kotlin` / `syntect`**
  - `tmp/.../src/parsing/syntax_definition.rs:10` — `use regex_syntax::escape`

#### `regex_syntax::hir::Class` — 1 edges, 1 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/nfa/mod.rs:6` — `use regex_syntax::hir::Class`

#### `regex_syntax::hir::ClassBytesRange` — 1 edges, 1 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/nfa/mod.rs:6` — `use regex_syntax::hir::ClassBytesRange`

#### `regex_syntax::hir::ClassUnicodeRange` — 1 edges, 1 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/nfa/mod.rs:6` — `use regex_syntax::hir::ClassUnicodeRange`

#### `regex_syntax::hir::Dot` — 2 edges, 2 caller crates

- **`logos-kotlin` / `logos`**
  - `tmp/.../logos-codegen/src/pattern.rs:4` — `use regex_syntax::hir::Dot`
- **`logos-kotlin` / `logos-codegen`**
  - `tmp/.../src/pattern.rs:4` — `use regex_syntax::hir::Dot`

#### `regex_syntax::hir::Hir` — 4 edges, 3 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/nfa/mod.rs:6` — `use regex_syntax::hir::Hir`
  - `tmp/.../src/lexer/re/mod.rs:1` — `use regex_syntax::hir::Hir`
- **`logos-kotlin` / `logos`**
  - `tmp/.../logos-codegen/src/pattern.rs:4` — `use regex_syntax::hir::Hir`
- **`logos-kotlin` / `logos-codegen`**
  - `tmp/.../src/pattern.rs:4` — `use regex_syntax::hir::Hir`

#### `regex_syntax::hir::HirKind` — 3 edges, 3 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/nfa/mod.rs:6` — `use regex_syntax::hir::HirKind`
- **`logos-kotlin` / `logos`**
  - `tmp/.../logos-codegen/src/pattern.rs:4` — `use regex_syntax::hir::HirKind`
- **`logos-kotlin` / `logos-codegen`**
  - `tmp/.../src/pattern.rs:4` — `use regex_syntax::hir::HirKind`

#### `regex_syntax::hir::Literal` — 1 edges, 1 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/nfa/mod.rs:6` — `use regex_syntax::hir::Literal`

#### `regex_syntax::hir::Repetition` — 1 edges, 1 caller crates

- **`lalrpop-kotlin` / `lalrpop`**
  - `tmp/.../src/lexer/nfa/mod.rs:6` — `use regex_syntax::hir::Repetition`

### Re-export surface — 10 `pub use` total

#### In `mod.rs` / `lib.rs` (10)

These are the canonical re-export sites the migration rule targets. When the upstream `mod.rs` line is `pub use <elsewhere>::<Name>`, downstream Kotlin ports should target `<elsewhere>::<Name>` directly, never the alias.

- `src/ast/mod.rs:6` — `pub use crate::ast::visitor::visit`
- `src/ast/mod.rs:6` — `pub use crate::ast::visitor::Visitor`
- `src/hir/mod.rs:18` — `pub use crate::hir::visitor::visit`
- `src/hir/mod.rs:18` — `pub use crate::hir::visitor::Visitor`
- `src/hir/mod.rs:18` — `pub use crate::unicode::CaseFoldError`
- `src/lib.rs:15` — `pub use crate::error::Error`
- `src/lib.rs:15` — `pub use crate::parser::parse`
- `src/lib.rs:15` — `pub use crate::parser::Parser`
- `src/lib.rs:15` — `pub use crate::parser::ParserBuilder`
- `src/lib.rs:15` — `pub use crate::unicode::UnicodeWordError`
